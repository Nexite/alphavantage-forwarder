generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Ip {
  ip          String   @id
  timesCalled Int
  lastCall    DateTime
}

model Ticker {
  name        String   @id
  timesCalled Int
  lastCall    DateTime
}
